
apiVersion: v1
kind: Service
metadata:
  name: web-local
  namespace: demo
spec:
  type: LoadBalancer
  externalTrafficPolicy: Local
  selector: { app: web }
  ports:
  - name: http
    port: 80
    targetPort: 8080
